---
// KailaEdit's own address, and the canonical one. The root serves the same
// page today; when a second product arrives the root becomes the page about
// Kaila itself and this route carries on unchanged.
import Landing from '../../components/Landing.astro'
---

<Landing lang="en" />
